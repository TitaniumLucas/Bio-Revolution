local event_handler = require("__core__.lualib.event_handler")

--- Control Scripts ---
event_handler.add_lib(require("control.freeplay-starting-items"))

--- Tests ---
-- event_handler.add_lib(require("unfinished.tests.control-hidden-techs"))
